[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "sic-advisor-core"
version = "0.3.0"
requires-python = ">=3.12"
description = "Portable candidate OULAD features and safe artifact inspection; contract 1.0.0"
[tool.setuptools.packages.find]
where = ["src"]
