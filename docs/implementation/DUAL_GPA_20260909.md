# Dual GPA and academic demo migration — 2026-09-09

## Scope completed

Student self-reported preview supports POL-01-demo: separate weighted GPA on scales 10 and 4. Convert individual course scores before averaging on scale 4. Preview explicitly assumes highest score per course and all entered courses count toward GPA. Data is not persisted. DEMO-1 remains the default API policy for backward compatibility; the UI offers both policies.

Shared implementation: packages/advisor_core/src/advisor_core/academic_demo_policy.py. Backend: SelfReportedPreview and self_reported_preview in backend/app/api.py. UI: frontend/src/StudentInput.tsx. Additive policy selector reflected in OpenAPI snapshot and generated frontend types. No migration or contract 1.0.0 artifact change.

## Dataset audit

Read-only ml/scripts/audit_academic_demo.py audits supplied synthetic_academic_dataset without reading user credentials. Observed 1,000 students, 24,370 enrollments; 145 missing final scores; 3,072 passing graded rows excluded from GPA require explanation. Catalog totals 139 credits, mandatory courses 123; declared graduation requirement 130 (123+7 selected elective credits). Duplicate-key and checked foreign-key violations: zero. Component/final score differences all within rounding tolerance 0.05. These checks do not establish ML readiness.

Product label may say demo; technical provenance must retain data_origin=synthetic. Do not overwrite or retrain existing OULAD final artifacts.

## Verification

- docker compose build api web: PASS.
- Test-ApplicationIsolated.ps1: 78 tests, 77 pass, one approved-model-absent skip; integration checks pass.
- Initial test run exposed stale OpenAPI snapshot inside image. Explicit snapshot review, regeneration and rebuild resolved it; subsequent full run passed.
- node frontend/scripts/generate-api-types.mjs --check: PASS.
- git diff --check: PASS (line-ending warnings only).

## Remaining work / gates

Full dataset import and stored-transcript POL-01 activation remain BLOCKED pending exact counts_for_gpa=false meaning and actual retake selection policy. Preview highest-score assumption is not approval of source semantics. Clarify whether excluded rows are non-GPA courses, superseded attempts, or both, and how to identify each.

Then implement versioned adapter with dry-run, immutable import provenance, dual-scale stored transcript and chat services, separate ML feature/target contracts and Colab workflow. No day-28 activity timestamps are available in this dataset; never synthesize missing events to claim real cutoff validity. Semester prediction must use only information available before the approved cutoff. Do not train or activate a replacement model until policy, labels and splits are validated.

Browser E2E and full migration/ML training NOT VERIFIED. Overall STATUS: PARTIAL.
